# Distro packaging stubs (ADR-040)

Enable `actor_mesh.distro_packages` / `KERROS_ACTOR_MESH_DISTRO=1` and
`allow_write` to render `.deb` DEBIAN/control and `.rpm` spec files here.

These are metadata stubs only — not published apt/yum packages.
