# Generated by KerrOS systemd timer packager (ADR-039).
# Example units — enable via actor_mesh.systemd_timers.allow_write=1
# or copy from this directory after rendering.

# kerros-acme-renew.service / kerros-acme-renew.timer are written at runtime
# when systemd_timers is enabled. This README documents the layout.

See `runtime/systemd_timers.py` and ADR-039.
