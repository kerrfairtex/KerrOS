# kerros-nats-operator Helm chart (ADR-047)

Enable `supercluster.helm_images` / `KERROS_ACTOR_MESH_HELM_IMAGES=1` and
`allow_write` to render Chart.yaml here. Package/push require explicit
gates — not a public OCI release.
