apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: natsbrokers.kerros.io
spec:
  group: kerros.io
  names:
    kind: NatsBroker
    plural: natsbrokers
    singular: natsbroker
  scope: Namespaced
  versions:
  - name: v1
    served: true
    storage: true
# Full OpenAPI schema in runtime/k8s_crd.NATS_BROKER_CRD
# note: foundation CRD stub — not a live operator-sdk project
