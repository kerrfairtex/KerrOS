# KerrOS Scope Policy

> Generated from `config/scope_policy.yaml` by `scripts/render_scope_policy.py` on **2026-08-04 20:13 UTC**.
>
> Do not edit this file by hand — re-run the renderer after changing `config/scope_policy.yaml`.
>
> Runtime allowlists (authorized targets/CIDRs and the deploy arm window) live in `config/scope.json` and are **not** listed here.

## Summary

| Class | Count | Gate |
|-------|------:|------|
| offensive | 16 | target allowlist (`scope.json`) |
| deploy | 8 | armed deploy window |
| **total gated** | **24** | |

## Defaults

| Key | Value |
|-----|-------|
| `deploy_arm_minutes` | `5` |
| `fail_closed` | `True` |
| `require_explicit_authorization` | `True` |
| `unknown_tools_are_passive` | `True` |

Policy version: **1**

## Offensive tools

Requires target allowlist in `config/scope.json` (exact host/IP or CIDR).

| Tool | Class |
|------|-------|
| `cert` | offensive |
| `dig` | offensive |
| `dnsenum` | offensive |
| `email_osint` | offensive |
| `geoint` | offensive |
| `geoip` | offensive |
| `headers` | offensive |
| `nikto` | offensive |
| `nmap` | offensive |
| `osint` | offensive |
| `ping` | offensive |
| `recon` | offensive |
| `reversedns` | offensive |
| `sigint` | offensive |
| `traceroute` | offensive |
| `whois` | offensive |

## Deploy tools

Requires `/scope arm-deploy` window (`deploy_armed_until` in scope.json).

| Tool | Class |
|------|-------|
| `cloudflare_deploy` | deploy |
| `github_create_repo` | deploy |
| `github_push` | deploy |
| `netlify_deploy` | deploy |
| `railway_deploy` | deploy |
| `stripe_trigger` | deploy |
| `supabase_migrate` | deploy |
| `vercel_deploy` | deploy |

## Deny messages

### `deploy_denied`

> BLOCKED: '{tool}' requires an armed deploy window. Use /scope arm-deploy <minutes> to authorize, then retry.

### `scope_denied`

> BLOCKED: '{target}' is not in the authorized scope. Use /scope add {target} to explicitly authorize it first.

## How to regenerate

```bash
python3 scripts/render_scope_policy.py
python3 scripts/render_scope_policy.py --check  # CI drift check
```

CLI: `/scope policy` (runtime summary) · `/scope policy export`
